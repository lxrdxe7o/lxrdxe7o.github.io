<section class="project-narrative" aria-label="Project narrative">
  <div class="project-narrative__body prose">
    <slot />
  </div>
</section>

<style>
  .project-narrative {
    padding-block: var(--space-section);
    border-bottom: 1px solid var(--color-border);
  }

  .project-narrative__body {
    max-inline-size: 72ch;
    margin-inline: auto;
    color: var(--color-fg-muted);
    font-size: var(--text-body-lg);
    line-height: var(--leading-relaxed);
  }

  .project-narrative__body :global(h2),
  .project-narrative__body :global(h3) {
    color: var(--color-fg);
    scroll-margin-top: calc(var(--header-block-size) + var(--space-6));
  }

  .project-narrative__body :global(h2) { margin-top: var(--space-12); }
  .project-narrative__body :global(h3) { margin-top: var(--space-8); }
  .project-narrative__body :global(p) { margin-top: var(--space-6); }
  .project-narrative__body :global(pre) { overflow-x: auto; margin-top: var(--space-8); }
</style>
